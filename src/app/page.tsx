export default function Home() {
  return (
    <div>
      <h1>
        Next.js + TypeScript + Tailwind CSS + ESLint + Prettier + Husky +
        Lint-Staged + Commitlint + Conventional Commits
      </h1>
    </div>
  );
}
